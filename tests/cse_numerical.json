{
    "__info": "Nonlinear numerical system containing deliberately repeated nonlinear expressions.",
    "dynamics": [
        {
            "expression": "x' = x**2 + exp(x + y) + sin(x + y)",
            "initial_value": "1"
        },
        {
            "expression": "y' = y**2 + 2 * exp(x + y) + 3 * sin(x + y)",
            "initial_value": "2"
        }
    ]
}